/**
 * Stub: the canonical insight extraction schema is in research/insight-schema.ts.
 *
 * When the pipeline architecture (task #5) is implemented, this file will
 * either re-export from the canonical location or contain the production
 * copy integrated with the Next.js build.
 */
